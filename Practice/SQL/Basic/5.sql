-- String matching
SELECT * FROM CD.FACILITIES 
WHERE name LIKE '%Tennis%'

-- In SQL stick to using single quote marks; they represent beginning and end of string
-- % for wildcard, _ for matching single letter